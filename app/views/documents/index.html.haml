%h1 Listing documents

%table.table.table-hover
  %tr
    %th Case number
    %th Document
    %th Date opened
    %th Case name
    %th Status
    %th
    %th
    %th
  %tbody
    - @documents.each do |document|
      %tr
        %td= document.case_number
        %td= link_to "Click to view PDF", document.filepicker_url
        %td= document.date_opened.strftime("%m/%d/%y")
        %td= document.case_name
        %td= document.paid ? "Paid" : "Under Review"
        %td= link_to 'Show', document
        %td= link_to 'Edit', edit_document_path(document)
        %td= link_to 'Destroy', document, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Document', new_document_path
