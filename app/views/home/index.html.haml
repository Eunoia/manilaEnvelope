%h1 Welcome to Manila Envelope. 
%div
  = raw @blog.body
- if current_user
  = link_to 'Upload new document', new_document_path, class: 'btn'
- else
  = link_to 'Sign Up', new_user_registration_path, class: 'btn'
